# pmbootstrap 配置文件 —— 仅供参考（了解各配置项含义）
# 实际初始化由 init-pmbootstrap.sh 通过 "printf | pmbootstrap init" 完成，
# 不直接部署本文件（pmbootstrap 3.9.0 cfg 格式已变更，不向后兼容）
#
# HOME=/work/pmbootstrap → 配置路径：/work/pmbootstrap/.config/pmbootstrap.cfg
#
# 如需修改配置，请在容器内运行：
#   su -s /bin/sh pmbuild -c "export HOME=/work/pmbootstrap; pmbootstrap config <key> <value>"
[pmbootstrap]
aports = /work/pmbootstrap/.local/var/pmbootstrap/cache_git/pmaports
ccache_size = 5G
channel = edge
device = google-kukui
extra_packages =
hostname = postmarketos
is_default_channel = False
jobs = 4
kernel = postmarketos-mediatek-mt81
keymap =
locale = en_US.UTF-8
nonfree_firmware = True
nonfree_userland = False
ssh_keys = False
ssh_key_glob = ~/.ssh/id_*.pub
timezone = UTC
ui = plasma-mobile
ui_extras = False
user = user
work = /work/pmbootstrap/.local/var/pmbootstrap

[providers]

[mirrors]
alpine = http://dl-cdn.alpinelinux.org/alpine/
alpine_custom = none
pmaports = http://mirror.postmarketos.org/postmarketos/
pmaports_custom = none
systemd = http://mirror.postmarketos.org/postmarketos/extra-repos/systemd/
systemd_custom = none
